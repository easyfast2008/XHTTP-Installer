# XHTTP-Installer
🚀 One-command VLESS+XHTTP+TLS proxy installer for Ubuntu with free Vercel/Netlify CDN relay. Hides server IP, auto-installs platform CLIs, smart auto-fix for SSL/firewall/ENV issues, end-to-end config validation with xray-knife, auto-retry on failure, full logging. No manual debugging
